diff -ru /mnt/data/cmake_fixed_orig/NOBS_CLiP/BOLT/CMakeLists.txt /mnt/data/cmake_fixed_work/NOBS_CLiP/BOLT/CMakeLists.txt
--- /mnt/data/cmake_fixed_orig/NOBS_CLiP/BOLT/CMakeLists.txt	2026-01-22 16:25:12.000000000 +0000
+++ /mnt/data/cmake_fixed_work/NOBS_CLiP/BOLT/CMakeLists.txt	2026-01-22 17:17:46.882476901 +0000
@@ -83,8 +83,11 @@
 option(BOLT_USE_64BIT_INDEX  "Use 64-bit indexing"                 ${_bolt_64bit_default})
 
 # Math backend selection
-set(BOLT_MATH_BACKEND "mkl" CACHE STRING "Math backend: mkl, cuda (future)")
-set_property(CACHE BOLT_MATH_BACKEND PROPERTY STRINGS "mkl" "cuda")
+#
+# "none" is a stub backend that provides a boltMathBackend INTERFACE target.
+# Use it for CI and for development environments without MKL installed.
+set(BOLT_MATH_BACKEND "mkl" CACHE STRING "Math backend: mkl, none, cuda (future)")
+set_property(CACHE BOLT_MATH_BACKEND PROPERTY STRINGS "mkl" "none" "cuda")
 
 # Backend-specific options
 set(BOLT_MKL_THREADING "" CACHE STRING "MKL threading model: gomp, iomp5, tbb")
diff -ru /mnt/data/cmake_fixed_orig/NOBS_CLiP/BOLT/us/mkl/CMakeLists.txt /mnt/data/cmake_fixed_work/NOBS_CLiP/BOLT/us/mkl/CMakeLists.txt
--- /mnt/data/cmake_fixed_orig/NOBS_CLiP/BOLT/us/mkl/CMakeLists.txt	2026-01-22 16:23:48.000000000 +0000
+++ /mnt/data/cmake_fixed_work/NOBS_CLiP/BOLT/us/mkl/CMakeLists.txt	2026-01-22 17:20:51.577670902 +0000
@@ -16,9 +16,11 @@
 # Validate MKLROOT
 # =============================================================================
 if(NOT DEFINED ENV{MKLROOT})
-    message(FATAL_ERROR 
-        "MKLROOT environment variable is not set.\n"
-        "Please source the Intel oneAPI setvars.sh/bat or set MKLROOT manually.")
+    message(FATAL_ERROR
+        "MKL backend selected but MKLROOT is not set.\n"
+        "Linux: source /opt/intel/oneapi/setvars.sh\n"
+        "Windows: run Intel oneAPI setvars.bat (or use the Intel oneAPI command prompt)\n"
+        "Or select the stub backend: -DBOLT_MATH_BACKEND=none")
 endif()
 
 set(MKL_ROOT "$ENV{MKLROOT}")
Only in /mnt/data/cmake_fixed_work/NOBS_CLiP/BOLT/us: none
diff -ru /mnt/data/cmake_fixed_orig/NOBS_CLiP/core/CMakeLists.txt /mnt/data/cmake_fixed_work/NOBS_CLiP/core/CMakeLists.txt
--- /mnt/data/cmake_fixed_orig/NOBS_CLiP/core/CMakeLists.txt	2026-01-22 16:21:39.000000000 +0000
+++ /mnt/data/cmake_fixed_work/NOBS_CLiP/core/CMakeLists.txt	2026-01-22 17:23:46.088854788 +0000
@@ -8,30 +8,85 @@
 # =============================================================================
 # Main Executable
 # =============================================================================
-add_executable(nobs-clip
+
+# -----------------------------------------------------------------------------
+# Source discovery / validation
+#
+# This CMake project is sometimes shared as "CMakeLists-only" snapshots where
+# the source tree is not present. To make configuration behavior explicit:
+#
+# - Main.cpp is REQUIRED (the executable cannot exist without at least one TU).
+# - All other files are validated and either:
+#     - FATAL_ERROR if NOBS_STRICT_SOURCES=ON
+#     - WARNING + skipped if NOBS_STRICT_SOURCES=OFF
+#
+# The default for NOBS_STRICT_SOURCES is:
+# - ON when the expected module directories exist
+# - OFF when they do not (typical of a CMakeLists-only snapshot)
+# -----------------------------------------------------------------------------
+set(_nobs_strict_default OFF)
+if(EXISTS "${CMAKE_SOURCE_DIR}/modules/classes" OR EXISTS "${CMAKE_SOURCE_DIR}/modules/sensor_models")
+    set(_nobs_strict_default ON)
+endif()
+option(NOBS_STRICT_SOURCES "Fail configure when expected source files are missing" ${_nobs_strict_default})
+
+set(_nobs_clip_required_sources
     Main.cpp
-    
+)
+
+set(_nobs_clip_optional_sources
     # Trajectory templates
-    ${CMAKE_SOURCE_DIR}/modules/classes/trajectory_templates/segments/ASegment.cpp
-    ${CMAKE_SOURCE_DIR}/modules/classes/trajectory_templates/templates/LadderTemplate.cpp
-    ${CMAKE_SOURCE_DIR}/modules/classes/trajectory_templates/templates/SimpleLadderTemplate.cpp
-    ${CMAKE_SOURCE_DIR}/modules/classes/trajectory_templates/templates/SimpleOrbitTemplate.cpp
-    ${CMAKE_SOURCE_DIR}/modules/classes/trajectory_templates/templates/TemplateExtraBoundsFactory.cpp
-    ${CMAKE_SOURCE_DIR}/modules/classes/trajectory_templates/templates/TemplateFactory.cpp
-    
+    "${CMAKE_SOURCE_DIR}/modules/classes/trajectory_templates/segments/ASegment.cpp"
+    "${CMAKE_SOURCE_DIR}/modules/classes/trajectory_templates/templates/LadderTemplate.cpp"
+    "${CMAKE_SOURCE_DIR}/modules/classes/trajectory_templates/templates/SimpleLadderTemplate.cpp"
+    "${CMAKE_SOURCE_DIR}/modules/classes/trajectory_templates/templates/SimpleOrbitTemplate.cpp"
+    "${CMAKE_SOURCE_DIR}/modules/classes/trajectory_templates/templates/TemplateExtraBoundsFactory.cpp"
+    "${CMAKE_SOURCE_DIR}/modules/classes/trajectory_templates/templates/TemplateFactory.cpp"
+
     # Sensor models
-    ${CMAKE_SOURCE_DIR}/modules/sensor_models/GaussianSensor/GaussianSensor.cpp
-    ${CMAKE_SOURCE_DIR}/modules/sensor_models/LLGaussianSensor/LLGaussianSensor.cpp
-    ${CMAKE_SOURCE_DIR}/modules/sensor_models/SensorFactory.cpp
-    ${CMAKE_SOURCE_DIR}/modules/sensor_models/LLSensorFactory.cpp
-    ${CMAKE_SOURCE_DIR}/modules/sensor_models/sensor_penalties/GaussianPenalty.cpp
-    ${CMAKE_SOURCE_DIR}/modules/sensor_models/sensor_penalties/IdentityPenalty.cpp
-    ${CMAKE_SOURCE_DIR}/modules/sensor_models/sensor_penalties/PenaltyFactory.cpp
-    
+    "${CMAKE_SOURCE_DIR}/modules/sensor_models/GaussianSensor/GaussianSensor.cpp"
+    "${CMAKE_SOURCE_DIR}/modules/sensor_models/LLGaussianSensor/LLGaussianSensor.cpp"
+    "${CMAKE_SOURCE_DIR}/modules/sensor_models/SensorFactory.cpp"
+    "${CMAKE_SOURCE_DIR}/modules/sensor_models/LLSensorFactory.cpp"
+    "${CMAKE_SOURCE_DIR}/modules/sensor_models/sensor_penalties/GaussianPenalty.cpp"
+    "${CMAKE_SOURCE_DIR}/modules/sensor_models/sensor_penalties/IdentityPenalty.cpp"
+    "${CMAKE_SOURCE_DIR}/modules/sensor_models/sensor_penalties/PenaltyFactory.cpp"
+
     # Common utilities
-    ${CMAKE_SOURCE_DIR}/common/parser/ConfigParser.cpp
+    "${CMAKE_SOURCE_DIR}/common/parser/ConfigParser.cpp"
 )
 
+set(_nobs_clip_sources "")
+foreach(_src IN LISTS _nobs_clip_required_sources)
+    set(_abs "${CMAKE_CURRENT_SOURCE_DIR}/${_src}")
+    if(NOT EXISTS "${_abs}")
+        message(FATAL_ERROR "Required source not found: ${_abs}")
+    endif()
+    list(APPEND _nobs_clip_sources "${_abs}")
+endforeach()
+
+set(_nobs_clip_missing_sources "")
+foreach(_src IN LISTS _nobs_clip_optional_sources)
+    if(EXISTS "${_src}")
+        list(APPEND _nobs_clip_sources "${_src}")
+    else()
+        list(APPEND _nobs_clip_missing_sources "${_src}")
+    endif()
+endforeach()
+
+if(_nobs_clip_missing_sources)
+    list(JOIN _nobs_clip_missing_sources "\n  " _nobs_clip_missing_msg)
+    if(NOBS_STRICT_SOURCES)
+        message(FATAL_ERROR
+            "The following sources listed by core/CMakeLists.txt were not found:\n  ${_nobs_clip_missing_msg}\n"
+            "Set -DNOBS_STRICT_SOURCES=OFF to allow configuring with missing sources.")
+    else()
+        message(WARNING "Skipping missing sources listed by core/CMakeLists.txt:\n  ${_nobs_clip_missing_msg}")
+    endif()
+endif()
+
+add_executable(nobs-clip ${_nobs_clip_sources})
+
 # =============================================================================
 # Include Directories
 # =============================================================================
diff -ru /mnt/data/cmake_fixed_orig/NOBS_CLiP/sim/snopt/CMakeLists.txt /mnt/data/cmake_fixed_work/NOBS_CLiP/sim/snopt/CMakeLists.txt
--- /mnt/data/cmake_fixed_orig/NOBS_CLiP/sim/snopt/CMakeLists.txt	2026-01-22 07:37:16.000000000 +0000
+++ /mnt/data/cmake_fixed_work/NOBS_CLiP/sim/snopt/CMakeLists.txt	2026-01-22 17:22:20.455707365 +0000
@@ -8,18 +8,28 @@
 # Simulated SNOPT library - provides stub implementations for testing
 # without requiring a real SNOPT license
 
-add_library(snoptProblem STATIC
+set(_nobs_snopt_stub_sources
     # Add your simulated SNOPT source files here
     # snoptProblem.cpp
     # snoptStubs.cpp
 )
 
-# Make this a header-only interface if you only have headers
-# add_library(snoptProblem INTERFACE)
+if(_nobs_snopt_stub_sources)
+    add_library(snoptProblem STATIC ${_nobs_snopt_stub_sources})
+else()
+    add_library(snoptProblem INTERFACE)
+endif()
 
-target_include_directories(snoptProblem PUBLIC
-    "${CMAKE_CURRENT_SOURCE_DIR}"
-)
+get_target_property(_nobs_snopt_type snoptProblem TYPE)
+if(_nobs_snopt_type STREQUAL "INTERFACE_LIBRARY")
+    target_include_directories(snoptProblem INTERFACE
+        "${CMAKE_CURRENT_SOURCE_DIR}"
+    )
+else()
+    target_include_directories(snoptProblem PUBLIC
+        "${CMAKE_CURRENT_SOURCE_DIR}"
+    )
+endif()
 
 # If the simulated SNOPT needs BOLT math:
 # target_link_libraries(snoptProblem PUBLIC boltMathBackend)
diff -ru /mnt/data/cmake_fixed_orig/NOBS_CLiP/testutils/CMakeLists.txt /mnt/data/cmake_fixed_work/NOBS_CLiP/testutils/CMakeLists.txt
--- /mnt/data/cmake_fixed_orig/NOBS_CLiP/testutils/CMakeLists.txt	2026-01-22 07:36:35.000000000 +0000
+++ /mnt/data/cmake_fixed_work/NOBS_CLiP/testutils/CMakeLists.txt	2026-01-22 17:21:38.775521902 +0000
@@ -5,24 +5,43 @@
 
 message(STATUS "Configuring test utilities...")
 
-# Test utilities library - provides common test fixtures and helpers
-add_library(nobs_testutils STATIC
+# Test utilities library - provides common test fixtures and helpers.
+#
+# If this library has no sources, use INTERFACE to avoid generator/toolchain
+# issues with empty STATIC archives.
+set(_nobs_testutils_sources
     # Add your test utility source files here
     # TestFixtures.cpp
     # TestHelpers.cpp
 )
 
-# If no sources yet, make it INTERFACE instead
-# add_library(nobs_testutils INTERFACE)
-
-target_include_directories(nobs_testutils PUBLIC
-    "${CMAKE_CURRENT_SOURCE_DIR}"
-    "${CMAKE_SOURCE_DIR}"
-)
-
-target_link_libraries(nobs_testutils PUBLIC
-    boltMathBackend
-    nobs_boost_headers
-)
+if(_nobs_testutils_sources)
+    add_library(nobs_testutils STATIC ${_nobs_testutils_sources})
+else()
+    add_library(nobs_testutils INTERFACE)
+endif()
+
+get_target_property(_nobs_testutils_type nobs_testutils TYPE)
+if(_nobs_testutils_type STREQUAL "INTERFACE_LIBRARY")
+    target_include_directories(nobs_testutils INTERFACE
+        "${CMAKE_CURRENT_SOURCE_DIR}"
+        "${CMAKE_SOURCE_DIR}"
+    )
+
+    target_link_libraries(nobs_testutils INTERFACE
+        boltMathBackend
+        nobs_boost_headers
+    )
+else()
+    target_include_directories(nobs_testutils PUBLIC
+        "${CMAKE_CURRENT_SOURCE_DIR}"
+        "${CMAKE_SOURCE_DIR}"
+    )
+
+    target_link_libraries(nobs_testutils PUBLIC
+        boltMathBackend
+        nobs_boost_headers
+    )
+endif()
 
 message(STATUS "Test utilities configured")
