# archives/bundles/

These are optional convenience bundles.

- `code/StableHashMap_all_latest_with_copies_and_optionB.zip`
  - Backup snapshot of the *full code history* (latest + copies + option B).
- `bench/benchmark_FatPHashMap_slim_bundle.zip`
  - Standalone-ish benchmark bundle for quick reproduction.

No other legacy bundles are kept here to avoid duplication.
