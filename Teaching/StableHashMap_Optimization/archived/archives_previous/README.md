# archives/

This folder is intentionally *small*.

It holds:

- `bundles/` — a couple of convenience zips (optional backups)
- `drafts/` — dated copies of older narrative revisions (kept out of `docs/`)
- `system/` — machine notes that help interpret benchmark numbers

Everything else (old bundles, intermediate zips, duplicate exports) has been removed to avoid archive noise.
