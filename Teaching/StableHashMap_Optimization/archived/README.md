# Archived material

This folder is intentionally a catch‑all.

It contains:
- earlier drafts / deprecated docs from previous iterations
- old exports (docx/pdf)
- intermediate bundles and build artifacts
- prior benchmark output logs

The current canonical docs live in `../docs/`.
